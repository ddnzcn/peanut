#ifndef PLATFORM_ASSET_PATH_HPP
#define PLATFORM_ASSET_PATH_HPP

#include <string>

namespace platform
{

std::string ResolveAssetPath(const std::string &relativePath);

} // namespace platform

#endif
